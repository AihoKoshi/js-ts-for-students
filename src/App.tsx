import React from 'react';
import './App.css';

function App() {

    return (
        <div>
            --------------
        </div>

    )

    // const familia = ['Julia', 'Adrian', 'Kira'];
    // const familia2 = [{name: 'Julia'}, {name: 'Adrian'}, {name: 'Kira'}];
    // const familiaList = familia.map((f, index) => <li key={index}>{f}</li>)
    // const familiaList2 = familia2.map((f, index) => <li key={index}>{index}{f.name}</li>)
    // return (
    //     <div className="App">
    //         <ul>
    //             <li><b>{familiaList}</b></li>
    //             <li><b>{familiaList2}</b></li>
    //         </ul>
    //     </div>
    // );
}

export default App;
